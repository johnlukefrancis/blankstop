{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "toast"],
  "permissions": [
    "core:default",
    "autostart:default"
  ]
}
